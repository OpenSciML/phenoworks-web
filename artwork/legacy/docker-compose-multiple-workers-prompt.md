Generated with the built-in image_gen tool. Edited asset: `static/img/docker-compose-architecture-reference-style.png`.

## Prompt

Use case: precise-object-edit. Asset type: PhenoWorks website software architecture infographic. Input image is the edit target. Preserve the existing wide three-column layout, colors, fonts, all other services, storage layers, entry points, arrows and footer. Change the center column step 4 from a single Celery Worker to a scalable group of workers. Exact heading: '4. Celery Workers'. Show three separate orange gear worker tiles labeled 'Worker 1', 'Worker 2', 'Worker N', with an ellipsis between 2 and N if space permits. Branch arrows from the RabbitMQ Queue to all workers to clearly communicate parallel consumers of a shared queue, not workers chained sequentially. Exact explanatory text: 'Multiple workers claim queued operations and process jobs in parallel.' Keep an orange dashed feedback connection from the worker group back to API operation records. Give this area sufficient room without overlap, retaining the rest of the architecture. Make the queue explanatory text neutral: 'Tasks are sent to the shared queue'. All text must remain crisp and readable. Do not change any other content.
